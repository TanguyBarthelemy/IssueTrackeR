% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sort_issues.R
\name{simple_sort}
\alias{simple_sort}
\title{Sorting issues}
\usage{
simple_sort(issues, sorting_variables, milestones, ...)
}
\arguments{
\item{issues}{a \code{IssuesTB} object.}

\item{sorting_variables}{a list of variable (represented by couples of
\code{object} and \code{field} in the form
\code{c(object = "object_1", field = "field_1")})}

\item{milestones}{a list representing milestones with simpler structure (with
title, description and due_on).}

\item{\dots}{Additional arguments passed to \code{\link{get_milestones}}.
Only used if \code{milestones} is missing.}
}
\value{
the same list of issues as \code{issues} (class \code{IssuesTB})
but in a sorted order.
}
\description{
Sort issues based on quantitative variables
}
\examples{

all_issues <- get_issues()
all_milestones <- get_milestones()

# with milestones
IssueTrackeR:::simple_sort(
    issues = all_issues,
    milestones = all_milestones,
    sorting_variables = list(
        c(object = "milestones", field = "due_on"),
        c(object = "issues", field = "created_at")
    )
)

# without milestones
IssueTrackeR:::simple_sort(
    issues = all_issues,
    sorting_variables = list(
        c(object = "milestones", field = "due_on"),
        c(object = "issues", field = "created_at")
    ),
    source = "local"
)

}
\keyword{internal}
