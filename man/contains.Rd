% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_issues.R
\name{body_contains}
\alias{body_contains}
\alias{body_contains.IssueTB}
\alias{body_contains.IssuesTB}
\alias{body_contains.default}
\alias{title_contains}
\alias{title_contains.IssueTB}
\alias{title_contains.IssuesTB}
\alias{title_contains.default}
\alias{labels_contains}
\alias{labels_contains.IssueTB}
\alias{labels_contains.IssuesTB}
\alias{milestone_contains}
\alias{milestone_contains.IssueTB}
\alias{milestone_contains.IssuesTB}
\title{Text in body of an issue(s)}
\usage{
body_contains(x, ...)

\method{body_contains}{IssueTB}(x, values, logic_gate = c("AND", "OR"), ...)

\method{body_contains}{IssuesTB}(x, ...)

\method{body_contains}{default}(x, ...)

title_contains(x, ...)

\method{title_contains}{IssueTB}(x, values, logic_gate = c("AND", "OR"), ...)

\method{title_contains}{IssuesTB}(x, ...)

\method{title_contains}{default}(x, ...)

labels_contains(x, ...)

\method{labels_contains}{IssueTB}(x, values, logic_gate = c("AND", "OR"), ...)

\method{labels_contains}{IssuesTB}(x, ...)

milestone_contains(x, ...)

\method{milestone_contains}{IssueTB}(x, values, logic_gate = c("AND", "OR"), ...)

\method{milestone_contains}{IssuesTB}(x, ...)
}
\arguments{
\item{x}{a \code{IssueTB} or \code{IssuesTB} object.}

\item{...}{Unused argument}

\item{values}{a \code{string}}

\item{logic_gate}{the logic operator which will aggregate the different
assertion related to values.}
}
\value{
a boolean specifying if the \code{pattern} is contained in the body.

a boolean specifying if the \code{values} are contained in the
title.

a boolean specifying if the \code{values} are contained in the
labels.

a boolean specifying if the \code{values} are contained in the
milestones.
}
\description{
Text in body of an issue(s)

Text in title of an issue

Labels in issue

Milestone in issue
}
\details{
For different values of \code{values}, the assertion will be constructed with
"OR gate" if the logic_gate argument is "OR" and "AND gate" if it is "AND".
This means that the call \code{body_contains(x = issue_1, values =
c("README", "package"), logic_gate = "OR")} will say whether the issue
contains the string "README" OR "package" in its body.
Whereas the call \code{body_contains(x = issue_1, values =
c("README", "package"), logic_gate = "AND")} will say whether the issue
contains the string "README" AND "package" in its body.

For different values of \code{values}, the assertion will be constructed with
"OR gate" if the logic_gate argument is "OR" and "AND gate" if it is "AND".
This means that the call \code{title_contains(x = issue_1, values =
c("README", "package"), logic_gate = "OR")} will say whether the issue
contains the string "README" OR "package" in its title.
Whereas the call \code{title_contains(x = issue_1, values =
c("README", "package"), logic_gate = "AND")} will say whether the issue
contains the string "README" AND "package" in its title.

For different values of \code{values}, the assertion will be constructed with
"OR gate" if the logic_gate argument is "OR" and "AND gate" if it is "AND".
This means that the call \code{labels_contains(x = issue_1, values =
c("medium", "unknown"), logic_gate = "OR")} will say whether the issue
contains the labels "medium" OR "unknown".
Whereas the call \code{labels_contains(x = issue_1, values =
c("medium", "unknown"), logic_gate = "AND")} will say whether the issue
contains the labels "medium" AND "unknown".

For different values of \code{values}, the assertion will be constructed with
"OR gate" if the logic_gate argument is "OR" and "AND gate" if it is "AND".
This means that the call
\code{
milestone_contains(
    x = issue_1,
    values = c("Fin juin 2024", "Fin juillet 2024"),
    logic_gate = "OR"
)}
will say whether the issue contains the milestones "Fin juin 2024" OR
"Fin juillet 2024".
Whereas the call
\code{
milestones_contain(
    x = issue_1,
    values = c("Fin juin 2024", "Fin juillet 2024"),
    logic_gate = "AND"
)}
will say whether the issue contains the milestones "Fin juin 2024" AND
"Fin juillet 2024".
}
\examples{
all_issues <- get_issues(type = "online", verbose = FALSE)
issue_1 <- all_issues[[1L]]
body_contains(x = issue_1, values = "README")
body_contains(
    x = issue_1,
    values = c("README", "package"),
    logic_gate = "AND"
)

all_issues <- get_issues(type = "online", verbose = FALSE)
issue_1 <- all_issues[[1L]]
title_contains(x = issue_1, values = "README")
title_contains(
    x = issue_1,
    values = c("README", "package"),
    logic_gate = "AND"
)

all_issues <- get_issues(type = "online", verbose = FALSE)
issue_1 <- all_issues[[1L]]
labels_contains(issue_1, "medium")
labels_contains(
    x = issue_1,
    values = c("medium", "unknown"),
    logic_gate = "AND"
)

all_issues <- get_issues(type = "online", verbose = FALSE)
issue_1 <- all_issues[[1L]]
milestone_contains(x = issue_1, values = "Fin juin 2024")
milestone_contains(
    x = issue_1,
    values = c("Fin juin 2024", "Fin juillet 2024"),
    logic_gate = "AND"
)

}
