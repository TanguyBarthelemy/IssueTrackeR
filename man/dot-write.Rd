% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write.R
\name{.write}
\alias{.write}
\title{Export an R Object to YAML}
\usage{
.write(
  x,
  dataset_dir = tempdir(),
  dataset_name = "object.yaml",
  overwrite = TRUE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{x}{An R object to export.}

\item{dataset_dir}{The destination directory where the YAML file will be
saved. By default, the system's temporary directory is used (\code{tempdir()}).}

\item{dataset_name}{The name of the output file (without extension).
By default, the name is \code{"object.yaml"}.}

\item{overwrite}{Logical indicating whether to overwrite the file if it
already exists. Defaults to \code{TRUE}.}

\item{verbose}{Logical indicating whether to display informative messages.
Defaults to \code{TRUE}.}

\item{...}{Currently not used.}
}
\value{
The function returns \strong{invisibly} the full path of the written YAML file.
If the file already exists and \code{overwrite = FALSE}, it returns \code{FALSE} without writing.
}
\description{
This function exports an R object (such as a list, vector, data.frame, etc.)
to a YAML file.
}
\details{
The functiobn automatically handles directory creation when the path doesn't
exist.
}
\examples{
my_list <- list(name = "John", age = 30, city = "Paris")
.write(my_list, dataset_name = "example_list")

my_df <- data.frame(id = 1:3, value = c("A", "B", "C"))
my_data_dir <- tempfile("data")
.write(my_df, dataset_dir = my_data_dir, dataset_name = "my_dataframe")

.write(my_list, dataset_name = "example_list", overwrite = FALSE)
}
\keyword{internal}
