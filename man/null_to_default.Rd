% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrangling_dataset_milestones.R
\name{null_to_default}
\alias{null_to_default}
\title{Replace NULL Values with a Default Value}
\usage{
null_to_default(x, default)
}
\arguments{
\item{x}{An R object}

\item{default}{The default value to replace \code{NULL} with.}
}
\value{
The input object \code{x} with all \code{NULL} values replaced by \code{default}.
}
\description{
Recursively replaces every \code{NULL} values in an object with a specified
default value.
}
\details{
If the input is a list, the function applies the replacement to each element
of the list (recursively).
}
\examples{
# Replace NULL with a numeric default
x <- list(a = 1, b = NULL, c = 3)
IssueTrackeR:::null_to_default(x, default = 0)

# Replace NULL with a character default
y <- list(name = "Alice", age = NULL, city = NULL)
IssueTrackeR:::null_to_default(y, default = "unknown")

# Nested list with NULL values
z <- list(
  id = 1,
  details = list(
    address = NULL,
    phone = "123-456-7890"
  )
)
IssueTrackeR:::null_to_default(z, default = NA)

# Atomic NULL value
IssueTrackeR:::null_to_default(NULL, default = FALSE)

}
\keyword{internal}
