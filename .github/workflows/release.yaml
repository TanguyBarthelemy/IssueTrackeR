name: Publish Release

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'New tag'
        required: true
        type: string

jobs:
  Prepare_new_release:

    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:

      - uses: TanguyBarthelemy/r-release-action@eab2b8ceefec5099438347ddee0ef1a26fff743d
        with:
          tag: ${{ github.event.inputs.tag }}
          gh_repo: "TanguyBarthelemy/IssueTrackeR"
          main-branch: "main"
          dev-branch: "develop"
